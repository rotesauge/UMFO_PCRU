
&НаСервере
Процедура pcru_ex_ПриСозданииНаСервереПеред(Отказ, СтандартнаяОбработка)
	Если Параметры.Отбор.Свойство("БНФОГруппаФинансовогоУчета") Тогда
		Параметры.Отбор.Удалить("БНФОГруппаФинансовогоУчета");//Вставить содержимое обработчика
КонецЕсли;

	Если Параметры.Отбор.Свойство("ВладелецЗначения") Тогда
		Параметры.Отбор.Удалить("ВладелецЗначения");//Вставить содержимое обработчика
КонецЕсли;
	
	КонецПроцедуры
