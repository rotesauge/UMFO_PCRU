
Функция pcru_ПодготовитьПараметрыПризнаниеВБУАрендныхПлатежей(СтруктураШапки, Отказ) Экспорт

	ПараметрыПроведения = Новый Структура;
	
	Запрос = Новый Запрос;
	
	Запрос.УстановитьПараметр("Ссылка", 	СтруктураШапки.Ссылка);
	Запрос.УстановитьПараметр("НачДата", 	СтруктураШапки.НачДата);
	Запрос.УстановитьПараметр("КонДата", 	СтруктураШапки.КонДата);
	
	Запрос.Текст = ТекстЗапросаРеквизиты();
	РеквизитыРегламентнойОперации = Запрос.Выполнить().Выгрузить();
	
	ПараметрыПроведения.Вставить("РеквизитыРегламентнойОперации", РеквизитыРегламентнойОперации);
	
	СтруктураПараметровКомпоновки = Новый Структура;
	СтруктураПараметровКомпоновки.Вставить("НачалоПериода",	СтруктураШапки.НачДата);
	СтруктураПараметровКомпоновки.Вставить("КонецПериода",  СтруктураШапки.КонДата);
	СтруктураПараметровКомпоновки.Вставить("Организация", 	СтруктураШапки.Организация);	
	СтруктураПараметровКомпоновки.Вставить("Содержание",	"Признание в БУ финансовой аренды");
	
	СтруктураПараметровКомпоновки.Вставить("НачалоМесяца",	НачалоМесяца(СтруктураШапки.НачДата));
	СтруктураПараметровКомпоновки.Вставить("НачалоКвартала",НачалоКвартала(СтруктураШапки.НачДата));
	СтруктураПараметровКомпоновки.Вставить("НачалоГода",  	НачалоГода(СтруктураШапки.НачДата));
	
	СтруктураПараметровКомпоновки.Вставить("КонецМесяца",	КонецМесяца(СтруктураШапки.НачДата));
	СтруктураПараметровКомпоновки.Вставить("КонецКвартала",	КонецКвартала(СтруктураШапки.НачДата));
	СтруктураПараметровКомпоновки.Вставить("КонецГода",  	КонецГода(СтруктураШапки.НачДата));
	
	ТаблицаДанныеПризнаниеВБУАрендныхПлатежей = ПолучитьДанныеКомпоновки(СтруктураШапки, СтруктураПараметровКомпоновки);
	
	ПараметрыПроведения.Вставить("ДанныеПризнаниеВБУАрендныхПлатежей", ТаблицаДанныеПризнаниеВБУАрендныхПлатежей);
	
	Возврат ПараметрыПроведения;
	
КонецФункции // pcru_ПодготовитьПараметрыПризнаниеВБУАрендныхПлатежей() 