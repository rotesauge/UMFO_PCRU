
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Для каждого Элемент Из ПараметрКоманды Цикл
		ЗаполнитьДату(Элемент);
	КонецЦикла; 
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьДату(Элемент)
	Об = Элемент.ПолучитьОбъект();
	Об.ДатаНачала = pcru_ex_WSWORKS.ПолучитьДатуПодписания(Об.Номер);
	Об.Записать();
КонецПроцедуры // ПолучитьАналитику()
