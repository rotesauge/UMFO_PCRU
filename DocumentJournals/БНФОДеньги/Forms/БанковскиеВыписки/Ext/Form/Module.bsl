
&НаСервере
Процедура pcru_ex_PCRU_ЗаписатьВыделенныеПослеНаСервере(Ссылки)
	Для Каждого текСсылка Из Ссылки Цикл
		Об = текСсылка.ПолучитьОбъект();
		Об.Записать();
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура pcru_ex_PCRU_ЗаписатьВыделенныеПосле(Команда)
	
	Ссылки =  Новый Массив;
	
	Для Каждого текСтр Из Элементы.Список.ВыделенныеСтроки Цикл
		Ссылки.Добавить(ТекСтр.Ссылка);
	КонецЦикла;
	pcru_ex_PCRU_ЗаписатьВыделенныеПослеНаСервере(Ссылки);
КонецПроцедуры
